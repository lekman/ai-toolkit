{
  "name": "@lekman/calendar-local",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local Office 365 calendar connector: Entra device-code auth and a Microsoft Graph day view for the planner skill.",
  "scripts": {
    "build": "echo 'calendar-local: nothing to build'",
    "typecheck": "bun --bun tsc --noEmit",
    "check": "bun run typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.2.0",
    "typescript": "^5.7.0"
  }
}
