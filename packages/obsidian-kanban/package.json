{
  "name": "obsidian-kanban",
  "private": true,
  "version": "0.5.0",
  "description": "Obsidian plugin: Dashboard.md as a board, one row per client",
  "scripts": {
    "install-plugin": "bun run scripts/install.ts",
    "test": "bun test",
    "check": "bun test"
  }
}
