{
  "name": "git",
  "description": "Git workflow: grouped conventional commits with QA and hook compliance, template-driven pull requests, merged-branch purge across local and remote, a pull request driven to green through reviewer threads and checks, and a hook that redirects a second PR against the default branch to a stack",
  "version": "0.4.0"
}
